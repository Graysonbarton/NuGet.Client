<Project>
  <PropertyGroup>
    <IsAotCompatible>$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', '$(NETCoreLegacyTargetFrameworkForSigning)'))</IsAotCompatible>
  </PropertyGroup>
  <Import Project="..\..\Directory.Build.targets" />
</Project>
